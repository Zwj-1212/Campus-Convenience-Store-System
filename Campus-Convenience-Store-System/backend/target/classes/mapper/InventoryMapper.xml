<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusconveniencestore.mapper.InventoryMapper">

    <select id="findAlerts" resultType="com.campusconveniencestore.vo.AlertVO">
        SELECT a.alertId, a.productId, p.productName, a.alertType, a.alertMsg, a.createTime, a.status
        FROM stockalert a
        LEFT JOIN product p ON a.productId = p.productId
        <where>
            <if test="status != null">
                AND a.status = #{status}
            </if>
        </where>
        ORDER BY a.createTime DESC, a.alertId DESC
    </select>

    <update id="markHandled">
        UPDATE stockalert SET status = 1 WHERE alertId = #{alertId}
    </update>

    <select id="countUnprocessed" resultType="long">
        SELECT COUNT(*) FROM stockalert WHERE status = 0
    </select>

    <select id="countByType" resultType="long">
        SELECT COUNT(*) FROM stockalert WHERE status = 0 AND alertType = #{alertType}
    </select>

</mapper>
